# Optional: comma-separated UptimeRobot Monitor-Specific API Keys for the static status page.
# This value is exposed to the browser in static builds. For public sites, prefer generating
# a static status.json with a server-side cron job instead of exposing API keys.
NEXT_PUBLIC_UPTIME_ROBOT_API_KEYS=
